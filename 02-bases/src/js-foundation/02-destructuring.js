const characters = ['Mario', 'Luigi', 'Toad', 'Peach'];

// Destructuramos el array de personajes
const [mario, luigi, toad, peach] = characters;

// Logeamos los personajes
console.table(characters);

// Logeamos uno a uno los personajes
console.log(mario);
console.log(luigi);
console.log(toad);
